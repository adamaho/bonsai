{
  "name": "@bonsai/svelte-button",
  "version": "0.0.1",
  "description": "A button component.",
  "main": "./src/index.ts",
  "typings": "./src/index.ts",
  "author": "adamaho",
  "exports": {
    ".": {
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src/index.ts"
  ],
  "scripts": {
    "build": "yarn build:types && yarn build:js",
    "build:js": "vite build",
    "build:types": "tsc --emitDeclarationOnly --declaration --declarationDir dist/types",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@bonsai/tsconfig": "workspace:*",
    "@tsconfig/svelte": "^3.0.0",
    "svelte": "^3.44.0",
    "typescript": "^4.5.5"
  },
  "peerDependencies": {
    "svelte": "^3.44.0"
  }
}
